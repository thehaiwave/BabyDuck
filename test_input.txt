program ExampleCompiler;
var 
    num: int;
    void fooFunc(arg1: int, arg2: int, arg3: float) [{}];
    void fooFunc2(arg1: int, arg2: int) [{}];
    void fooFunc3(arg1: int) [
          var 
            bar: float;
            bar2: int;

        {
        
        testFunction();
        parameterFunction(arg1, 3+4);
        
        while { } do (count < 20);
        while {
            i = num + 1;
        } do (num < 10);

        print(3+4);
        print(5+6, 7+8);

    }];


main
{
    expr1 = 5 + 3;
    expr2 = (7 + 2) > 4/2;
    
    if (condition) { };
    
    if (num != 10) {
        foo = 5;
    } else {
        foo = 10;
    };
    
    
    while { } do (count < 20);
    while {
        i = num + 1;
    } do (num < 10);

    print(3+4);
    print(5+6, 7+8);
}

end
